<header
  class="bg-primary sticky top-0 z-30 border-b border-black/20 py-1 shadow-md"
>
  <div class="container flex items-center justify-between">
    <a href="/" class="font-display text-secondary text-2xl">
      Helston Flora Day
    </a>
    <nav class="relative">
      <button class="text-white opacity-70 md:hidden" data-menu-button
        >Menu</button
      >
      <ul
        data-menu
        class="bg-quaternary absolute right-0 hidden w-60 font-bold text-white md:relative md:flex md:w-auto md:items-center md:bg-transparent md:text-sm"
      >
        {
          [
            { href: '/dates', title: 'Dates' },
            { href: '/the-dances', title: 'The Dances' },
            { href: '/on-the-day', title: 'On The Day' },
            { href: '/official-guide', title: 'Official Guide' },
            { href: '/gallery', title: 'Gallery' },
            { href: '/contact', title: 'Contact' },
          ].map(({ href, title }) => (
            <li>
              <a
                href={href}
                class="block p-3"
                class:list={
                  Astro.url.pathname.startsWith(href)
                    ? 'text-secondary'
                    : 'md:opacity-70'
                }
              >
                {title}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  const menuButton = document.querySelector('[data-menu-button]')
  const menu = document.querySelector('[data-menu]')

  menuButton?.addEventListener('click', () => {
    menu?.classList.toggle('hidden')
  })
</script>
