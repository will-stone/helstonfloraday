<header
  class="sticky top-0 z-30 border-b border-black/20 bg-primary py-1 shadow-md"
>
  <div class="container flex items-center justify-between">
    <h1 class="font-display text-2xl text-secondary">
      <a href="/">Helston Flora Day</a>
    </h1>
    <nav class="relative">
      <button class="text-white opacity-70 md:hidden" data-menu-button
        >Menu</button
      >
      <ul
        data-menu
        class="absolute right-0 hidden w-60 bg-quaternary font-bold text-white md:relative md:flex md:w-auto md:items-center md:bg-transparent md:text-sm md:opacity-70"
      >
        {
          [
            { href: '/dates', title: 'Dates' },
            { href: '/', title: 'Ths Dances' },
            { href: '/', title: 'On The Day' },
            { href: '/', title: 'Official Guide' },
            { href: '/', title: 'Gallery' },
            { href: '/', title: 'Contact' },
          ].map(({ href, title }) => (
            <li>
              <a href={href} class="block p-3">
                {title}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<script>
  const menuButton = document.querySelector('[data-menu-button]')
  const menu = document.querySelector('[data-menu]')

  menuButton?.addEventListener('click', () => {
    menu?.classList.toggle('hidden')
  })
</script>
